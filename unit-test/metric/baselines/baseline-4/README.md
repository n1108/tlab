对比故障时段与正常时段的统计值，计算变化幅度。

识别流程：
1. **数据清洗**：去掉最小 2 个和最大 2 个值（去尾平均）
2. **计算统计值**：P50（中位数）、P99（99 分位）
3. **对称比率判断**：
   ```python
   对称比率 = |故障值 - 正常值| / ((故障值 + 正常值) / 2)
   
   if 对称比率 < 0.05:  # 变化<5%
       视为正常，过滤
   else:
       标记为异常，保留分析
   ```

关键指标： P50（典型水平）+ P99（极端情况），双重验证确保准确性。